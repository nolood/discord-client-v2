import { type FC } from "react";
import { RegisterForm } from "~/widgets/register-form/ui";

const Register: FC = () => {
  return (
    <>
      <RegisterForm />
    </>
  );
};

export default Register;
